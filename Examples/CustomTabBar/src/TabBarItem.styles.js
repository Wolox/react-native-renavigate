import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  tab: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  },
  tabImage: {
    height: 24,
    resizeMode: 'contain',
    width: 24
  }
});
